export interface Sizes {
  s: string;
  m: string;
  l: string;
}

export interface Variants {
  light: string;
  dark: string;
  primary: string;
  secondary: string;
}

export interface InputVariants {
  tertionary: string;
  dark: string;
  white: string;

  primary: string;
  secondary: string;
}
